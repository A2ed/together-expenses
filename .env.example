# Required only for first startup; stored as a salted hash in the local database.
APP_PASSWORD=
PORT=4310
# Optional: alternatively configure these inside Settings.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4.1-mini
# Only enable when serving over HTTPS.
COOKIE_SECURE=false

# Optional display labels. Never enter a full card number.
HOUSEHOLD_NAME=Our household
CARD_NAME=Scotiabank Visa
CARD_LAST_FOUR=
